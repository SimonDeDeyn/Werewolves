import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.simondedeyn.werewolves",
  appName: "Werewolves",
  webDir: "dist",
  backgroundColor: "#0a120c",
};

export default config;
