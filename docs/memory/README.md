# Memory snapshot

A versioned copy of Claude Code's project memory notes for this repo, so the
context survives across machines and sessions. The live copies Claude actually
recalls from live in the local `~/.claude` memory store; these are a committed
mirror, kept in sync whenever a memory changes.

- [refresh-memory-mirror](refresh-memory-mirror.md) — after changing a memory, re-sync this mirror and push
- [werewolves-art-pipeline](werewolves-art-pipeline.md) — how role head/card art is named, sized, and dropped in
- [pokemon-card-layout-deferred](pokemon-card-layout-deferred.md) — finished 5:7 card layout parked in docs/, waiting on landscape card art
